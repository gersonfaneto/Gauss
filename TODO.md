# TODO

## Requirements

- Operations are only supported between matrices of size `N x N`, where $N \le 5$

- Each element of any matrix is an `integer` of size 8 bits.

- Implementations must be done through _paralelism_ to optmize the execution of
  the operations.

- Architecture *must* based on a _pipeline_ to make the processes more eficient.

## Interface

...

## Processor

- [ ] Perform memory operations with the matrices

    - [ ] Store a matrix into memory
    - [ ] Load a matrix from memory

- [ ] Perform arithmetic operations with the matrices

    - [ ] Sum two matrices
    - [ ] Subtract two matrices
    - [ ] Multiply two matrices
    - [ ] Multiply a matrix by a scalar
    - [ ] Find the determinant of a matrix
    - [ ] Find the transpose of a matrix
    - [ ] Find the inverse of a matrix
